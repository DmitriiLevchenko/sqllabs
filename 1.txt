1.Проаналізувати приклад універсального відношення.
Вирішити яка його колонка буде головним ключем, які поля обов'язкові для введення, які поля мають значення 
за замовчуванням, які поля мають область допустимих значень, які поля необхідно проіндексувати.

1.1 колонкою с головним ключем можем бути 'Код'
1.2 обов'язкові для введення: 'Назва', 'Ціна', 'Тема', 'Категорія'
1.3 оля мають значення за замовчуванням: 
 поле 'Новинка' yes
1.4 поля мають область допустимих значень: 'Формат','Категорія'
1.5 поля необхідно проіндексувати: 'Назва', 'Ціна'

2.Скласти SQL-script, який виконує:
a. Видалення попередньої версії бази даних
DROP DATABASE IF EXISTS database_name 
b.Створення бази даних
CREATE DATABASE IF NOT EXISTS database_name
c. Створення таблиці на основі універсального відношення. Команда для створення таблиці повинна містити 
головний ключ, обмеження типу null / not null, default, check.

use database_name;
CREATE TABLE IF NOT EXISTS table_name (
    id int NOT NULL AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    last_name VARCHAR(255) NOT NULL,
    age TINYINT CHECK(age >= 18),
    isClient BOOLEAN DEFAULT true
)

d.Створення додаткового індексу
USE database_name;

CREATE INDEX idx_name on table_name(name);

e.Завантаження даних в таблицю
USE database_name;

INSERT INTO table_name (name,last_name,age,isClient) VALUES
('Dmitry','Levchenko',18,0),
('Alexs','Durma',23,1)

3.
a. Додавання в таблицю нового текстового поля "Автор", розміром 15 символів

use database_name;

ALTER TABLE table_name ADD author VARCHAR(15) NOT NULL

b.Збільшення розміру текстового поля "Автор" до 20 символів
use database_name;

ALTER TABLE table_name MODIFY COLUMN author VARCHAR(20);
c. Видалення текстового поля "Автор" з таблиці
use database_name;

ALTER TABLE table_name DROP COLUMN author

d. Зміна порядку сортування індексу і призначення його унікальності
use database_name;

CREATE UNIQUE INDEX name_ind ON table_name (name DESC);

e. Видалення індексу
use database_name;

ALTER TABLE table_name DROP INDEX name_ind 


